请结合聊天上下文润色“我准备发送的草稿”。

我的母语：{MY_NATIVE}
对方母语：{PEER_NATIVE}
当前时间：{CURRENT_TIME}
聊天背景：{BACKGROUND}
聊天目的：{PURPOSE}
上下文状态：{CONTEXT_STATUS}

要求：
1. 保留草稿核心意思，不擅自增加承诺、邀约、道歉、金钱、暧昧或感情表态。
2. 给出3至5个自然程度和表达方式不同的版本。
3. 每个版本严格使用下面的 Markdown 结构：

### 版本 1 · 简短风格名
```reply
使用对方母语、可以直接发送的润色结果
```
**译文**
```translation
上面润色结果的“我的母语”准确译文
```

4. 后续版本依次使用“版本 2”“版本 3”等。每个版本必须包含一组 reply 代码块和紧跟其后的 translation 代码块。
5. reply 代码块内只能写可直接发送的文字；translation 代码块内只能写对应译文。不得在代码块内放标签、解释、引号或序号。
6. 各版本表达方式应明显不同，但不能改变草稿核心意图。
7. 不要输出 JSON，不要使用表格或引用块。

最近聊天记录（时间正序）：
{MESSAGES}

我的草稿：
{DRAFT}
