请结合附近聊天上下文，翻译“目标消息”。

我的母语：{MY_NATIVE}
对方母语：{PEER_NATIVE}
当前时间：{CURRENT_TIME}
上下文状态：{CONTEXT_STATUS}

要求：
1. 忠实原文，在不改变含义的前提下给出自然、符合聊天语境的译文。
2. 不增加原文没有的感情，不把普通表达翻成暧昧，不把委婉表达翻成明确承诺，不刻意美化冷淡或拒绝语气。
3. 严格使用下面的普通文本格式，不得使用 Markdown 代码块、反引号、表格或引用：
译文：自然、准确的译文
直译：只有在与自然译文差异明显时才提供；否则写“同上”
语气：一句简短说明；存在多义、俚语或省略时在这里说明
4. “译文：”后只写译文本身，不要加引号、序号或解释。
5. 不生成回复建议，不输出 JSON，不把译文放进代码块。

附近聊天上下文（时间正序）：
{MESSAGES}

目标消息：
{TARGET_MESSAGE}
