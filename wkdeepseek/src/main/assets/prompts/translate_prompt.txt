请结合附近聊天上下文，翻译“目标消息”。

我的母语：{MY_NATIVE}
对方母语：{PEER_NATIVE}
当前时间：{CURRENT_TIME}
上下文状态：{CONTEXT_STATUS}

要求：
1. 忠实原文，在不改变含义的前提下给出自然、符合聊天语境的译文。
2. 不增加原文没有的感情，不把普通表达翻成暧昧，不把委婉表达翻成明确承诺，不刻意美化冷淡或拒绝语气。
3. 译文必须放在回答最前面，让用户打开页面后第一眼就能看到。
4. 严格使用下面的 Markdown 格式：

## 译文
```translation
自然、准确的译文正文
```

仅当直译与自然译文明显不同时，再追加：
## 直译
```translation
更贴近字面的直译正文
```

最后可追加一行：
**语气：** 一句简短说明；存在多义、俚语或省略时在这里说明。

5. 代码块内只能写译文本身，不要添加标签、引号、序号或解释。
6. 不生成回复建议，不输出 JSON，不输出其他代码块。

附近聊天上下文（时间正序）：
{MESSAGES}

目标消息：
{TARGET_MESSAGE}
