<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="com.chat.partnerlist.PartnerListInitProvider"
            android:authorities="${applicationId}.partnerlist.init"
            android:exported="false"
            android:initOrder="90" />

        <activity
            android:name="com.chat.partnerlist.PartnerListActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:exported="false"
            android:screenOrientation="portrait" />
    </application>
</manifest>
