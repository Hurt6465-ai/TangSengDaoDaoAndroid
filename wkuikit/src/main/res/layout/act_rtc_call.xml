<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        RTC UI has been moved out of wkuikit into the root plugin module wkrtc.
        This stub prevents wkuikit ViewBinding from generating references to the WebRTC renderer classes
        if the old layout is still present after applying the changed-only patch.
    -->

</FrameLayout>
